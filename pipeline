pipeline{
    agent any
    stages{
        stage("for terraform){
            steps{
                sh "var/lib/jenkins/workspace/$(JOB_NAME)/main.tf
                sh "terraform init"
                sh "terraform validate"
                sh "terraform apply"
            }
        }
    }
}
